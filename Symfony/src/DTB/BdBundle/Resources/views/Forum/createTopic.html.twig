{% extends '::layout.html.twig' %}

{% block stylesheets %}
    <script type="text/javascript" src="{{asset('js/tiny_mce/tiny_mce.js')}}"></script>
{% endblock %}

{% block content %}
<style>
        textarea
        {
            width: 500px;
            height: 100px;
        }
</style>
<div style="text-align: center">
        {% if edit %}
        <form method="post" action="{{path('dtb_update_topic', {'id': topic.id})}}" novalidate>
        <legend>Editer un Topic</legend>
        {% else %}
        <form method="post" action="{{path('dtb_create_topic', {'id': forum.id})}}" novalidate>
        <legend>Créer un nouveau Topic</legend>
        {% endif %}
        <div align=center>
        {{form_widget(form)}}
        </div>
        <div class="form-actions">
            <input type="submit" class="btn btn-primary" />
        </div>
    </form>
</div>

<script type="text/javascript">
    tinyMCE.init({
		mode : "textareas", 
		theme : "advanced", 
		plugins : "advhr,insertdatetime,preview,save", 
		width : 1000,
                height: 300,
		theme_advanced_buttons1 : "bold,italic,underline,|,justifyleft,justifycenter,justifyright,|,fontselect,formatselect,|,sub,sup,|,undo,redo,|,bullist,numlist,|,outdent,indent,|,link,unlink,image,|,forecolor",
		theme_advanced_toolbar_location : "top",
		theme_advanced_toolbar_align : "left",
		theme_advanced_statusbar_location : "none",
		theme_advanced_resizing : true
	});
</script>
{% endblock %}