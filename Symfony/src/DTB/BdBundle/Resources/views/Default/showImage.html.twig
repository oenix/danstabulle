{% extends '::layout.html.twig' %}

{% block content %}
<h2>Futur interface de dessin pour l'image "{{image.title}}"</h2>
<div class="demo" imgID="{{image.id}}">
    <img src="{{asset('img/v' ~ image.id ~ '.jpg')}}" />
</div>
{% endblock %}