{% extends '::layout.html.twig' %}
{% block content %}
<style>
        textarea
        {
            width: 500px;
            height: 100px;
        }
</style>
<div style="text-align: center">
    <form method="post" action="{{path('dtb_bd_update', {'id': bd.id})}}">
        <legend>Edition de "{{bd.title}}"</legend>
        {{form_widget(form)}}
        <div class="form-actions">
            <input type="submit" class="btn btn-primary" />
        </div>
        </form>
    </div>
{% endblock %}