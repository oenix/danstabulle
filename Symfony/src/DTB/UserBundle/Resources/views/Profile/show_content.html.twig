
        <section id="Contenu" class="couverture span4">
        {% if user.ProfilePicture.webPath is defined  %}
            <img class="img-polaroid"  width="250" height="200" src="{{ asset(user.ProfilePicture.webPath) }}" >
        {% else %}
            <img class="img-polaroid" src="{{ "img/avatar.png" }}" >
        {% endif %}
        </section>
        <section id="Contenu" class="jumbotron masthead span8">

            <h1 style="text-transform : uppercase;">{{ user.username}}</h1>
            <p class="lead">{{ user.description}}</p>
            <a class="btn btn-large btn-success" id="Start">Projets</a>
            <a class="btn btn-large btn-warning" id="Start">Contacter</a>
        </section>

        <div class="span12">
            <h2>Informations</h2>
        </div>
        <div class="span2">

        </div>

        <div class="span8 pado">

            <table class="table table-striped table-hover table-bordered">
                <tr>
                    <td class="leader">Email</td>
                    <td class="lead">{{ user.email}}</td>
                </tr>
                <tr>
                    <td class="leader">Nom</td>
                    <td class="lead">{{ user.lastname | capitalize }}</td>
                </tr>
                <tr>
                    <td class="leader">Prénom</td>
                    <td class="lead">{{ user.firstname | capitalize }}</td>
                </tr>
                <tr>
                    <td class="leader">Ville</td>
                    <td class="lead">{{ user.city}}</td>
                </tr>
                <tr>
                    <td class="leader">Date de naissance</td>
                    {% if user.birthdate is defined %}
                    <td class="lead">{{ user.birthdate|date("m/d/Y")}}</td>
                    {% endif %}
                </tr>
                <tr>
                    <td class="leader">Facebook</td>
                    <td class="lead">{{ user.facebook}}</td>
                </tr>
                <tr>
                    <td class="leader">Twitter</td>
                    <td class="lead">{{ user.twitter}}</td>
                </tr>
                <tr>
                    <td class="leader">Skype</td>
                    <td class="lead">{{ user.skype}}</td>
                </tr>
            </table>


        </div>
    </div>