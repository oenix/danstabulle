<html>
    <body>
        <h1>Inscris toi (Version très basique)</h1>
        <form method="post" {{form_enctype(form)}}>
        {{ form_widget(form)}}
            <input type="submit" />
        </form>
        <h1>List des utilisateurs</h1>
        <table>
            <tr><th><b>Username</b></th><th><b>Password</b></th></tr>
        {% for user in users %}
            <tr><td>{{user.username}}</td><td>{{user.password}}</td></tr>
        {% endfor %}
        </table>
    </body>
</html>